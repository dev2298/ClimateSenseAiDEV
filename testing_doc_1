RetryHelper.ExecuteWithRetry(
                                   () => AccountMasterDAL.SyncDataToClient(primaryOperationId, clientId, out dsDataForSave),// Main action
                                   () => AccountMasterDAL.RollbackTransaction(primaryOperationId),         // Rollback action
                               primaryOperationId);
